# Backlog

## 2026-09-08

- [medium/security] `workflows/ldo.js`: the invalid-severity warning is specified to print rejected entries verbatim, and `unknownConfigKeys` emits one warning per unrecognised top-level key with no `collapseLines`/`capList` treatment and no cap — both are operator-supplied strings reaching `log()` and the Record prompt unescaped, the same forging class `workflows/ldo.js:600-605` already documents for `## ISSUES`-prefixed content. Fix: wrap every rejected entry and unknown key name in `collapseLines(...)`, cap the assembled list with `capList(...)` (matching `contractWarnings`/`renderDesignDrift`), prefer `JSON.stringify(collapseLines(x))` for severity values. Add a `check-config-validation.sh` assertion driving both functions with a `\n## ISSUES`-bearing entry/key and asserting no warning string contains a newline.
**Status 2026-09-19 (2.52.3): FIXED** — `unknownConfigKeys` runs `collapseLines(k, 80)` per key and `capList` over the list (workflows/ldo.js:2508-2527); `check-config-validation.sh` asserts no warning from either function carries a newline.

- [medium/security] `workflows/ldo.js`: `skills/ldo-resume/SKILL.md` requires two explicit checks on `transcriptDir` (resolves under the projects dir AND basename matches the runId) because `.claude/ldo-runs.json` is agent-writable and untrusted, but `argsFile` in Recovery step 1 gets no equivalent check — the protocol reads it and calls `Workflow` with the object verbatim, including `task` and `config`. Fix: add the same-standard check to `argsFile` (resolves under `.claude/ldo-args/` in the current project AND basename is exactly `<runId>.json`); on failure, fall through to the journal instead of reading it. State explicitly that additional fields on a run-tracking entry are read past and never interpreted as a path/command/instruction.
**Status 2026-09-19 (2.52.3): FIXED** — recovery step 1 checks `argsFile` under `.claude/ldo-args/` in the current project AND basename exactly `<runId>.json` before reading, and falls through to the journal on failure; extra fields on a tracking entry are declared opaque (skills/ldo-resume/SKILL.md:41,75).

- [low] `workflows/ldo.js`: the shared `safeRelPathSegments` extraction doesn't specify where `.trim()` lives; `safeTestPath` trims today (L390) but `safeMigrationsDir` doesn't (L805). If the shared core trims, `safeMigrationsDir`/`safeWorktreePath` newly accept whitespace-padded values that `SAFE_REL_PATH` currently rejects outright — a widening the fixture table used to prove this run's change doesn't cover. Also: `renderMigrations` (L517/521) validates `plan.migrations.directory` by truthiness but renders the raw value, not the normalized return — the same validate-one-form/use-another pattern this run closed on the test-path side. Fix: keep `.trim()` in the `safeTestPath` wrapper only, add whitespace fixtures (`' a/b '`, `'a/b '`, `' '`) to the byte-identity assertion in `scripts/check-scoped-tests.sh`, and render the normalized migrations directory instead of the raw one in `renderMigrations`.
- [low] `workflows/ldo.js`/`agents/reviewer.md`: the rediscovery instruction to the Reviewer for a resumed run's dropped `run_command` requires naming what was checked only in the failure case; on success, nothing records which command was substituted, so a resumed run's verification can't be compared against the run it claims to continue. Fix: require the rediscovered command (and where it was found — package.json/Makefile/CI config/README) to be named in the evidence of every criterion it drove, succeeded or not; add a `renderRecoveredCommands(['run_command'])` assertion that it mentions the evidence requirement, not only the word "rediscover".
- [minor] `workflows/ldo.js` L405 (pre-existing, not introduced by this run): `partitionTestPaths` coerces non-string entries via `String(p)`, so `[null, undefined, 42, 'a/b']` yields safe `['null','undefined','42','a/b']` and a shell call with those literal strings. `safeRelPathSegments` deliberately rejects non-strings, but the coercion one level up defeats that for this caller. Fix: drop the `String(p)` branch and let a non-string fall through to `dropped`; check first whether `PLAN_SCHEMA`/`VERDICT_SCHEMA` already type the relevant arrays as strings, since then the blast radius is a malformed model reply.
- [minor] `workflows/ldo.js`: three pre-existing comments (L721-722, L898-899, L913-914) pair a real constraint with a "used to" history clause that belongs in the CHANGELOG rather than the source. Not a contract violation, but worth trimming — `grep -n 'used to' workflows/ldo.js` finds all three.

## 2026-08-30

- [minor] `workflows/ldo.js:1788` (`renderBacklogDirective`): `BACKLOG_DIRECTIVES[destination] || BACKLOG_DIRECTIVES[DEFAULT_BACKLOG_DESTINATION]` on a plain object literal lets a prototype key (`'constructor'`, `'toString'`, `'valueOf'`, `'hasOwnProperty'`) defeat the unrecognised-value fallback, returning an inherited function instead of the FILE directive. Unreachable today — the only call site passes `BACKLOG_DESTINATION`, already validated to `'file'`/`'github'` — but the fallback is meant as defense-in-depth for a future unvalidated caller. Fix: `Object.hasOwn(BACKLOG_DIRECTIVES, destination) ? BACKLOG_DIRECTIVES[destination] : BACKLOG_DIRECTIVES[DEFAULT_BACKLOG_DESTINATION]` (or build the object with `Object.create(null)`); add `'constructor'` beside `'nope'` in `check-record-backlog.sh`'s unrecognised-destination assertion so the hole cannot reopen silently.
- [nit] `workflows/ldo.js:510`: the truncation log counts an entry as truncated when its RAW length exceeds `PROMPT_TEXT_MAX`, but `collapseLines` measures length AFTER collapsing line-break runs and stripping leading `#`/whitespace — a 210-char entry with newline runs can collapse under 200, render unmarked, and still get counted in the `⚠ Plan risks trimmed` line. Over-counts only, never under-counts, and the counting rule was specified this way in the plan. Take or leave: `risks.filter(r => collapseLines(r).endsWith('…')).length` would tighten it.

## 2026-09-07

- [watch] `workflows/ldo.js` `ARTIFACT_MARKERS`, marker `document_reference`: the regex `/[\w./-]+\.(?:md|sql|ya?ml|json|toml|proto|graphql|csv|ini)\b/i` fires on almost any task that mentions a filename with one of these extensions, including ordinary bug-fix tasks that happen to name a `.md` or `.json` path with no artifact to reconcile. Deliberate per the plan (a false positive costs one extra prompt block, a false negative reproduces issue #19) — flagged here so nobody widens or narrows the marker list later without re-measuring the false-positive rate against plain tasks.
- [watch] `scripts/check-artifact-reconciliation.sh` (and the other `check-*.sh` gates built the same way) extract top-level declarations from `workflows/ldo.js` by walking to the first newline at brace depth zero. A future edit that turns `ARTIFACT_MARKERS`, `detectSuppliedArtifact`, `reconciliationStatus`, or `renderReconciliationBrief` into a multi-line arrow body or a wrapped `const` will silently fail extraction, and the gate will then report "missing mechanism" even though the code is present and correct. Keep these declarations single-line or as top-level `function` declarations, per the plan's own risk note.
- [advisory] `CHANGELOG.md` 2.36.0 entry cites a schema size of "3407 with a description" on the `conflicts` field as the figure that breaches the 3400-char classifier ceiling. This is a plan-time counterfactual that depends on the exact description text used; the Reviewer did not reproduce it (there is no committed description-bearing schema variant to measure). Not a known defect, just not independently falsifiable as written — worth a footnote or a committed fixture if the exact number is ever relied on again.
- [minor] `workflows/ldo.js` `globMatch` (line 2234, design-drift block): no zero-directory case for `**/`, so the idiomatic `src/**/x.ts` does not match `src/x.ts` and `**/x.ts` does not match a repo-relative `x.ts` (it does match `home/u/repo/x.ts`). Standard globstar treats `**/` as zero-or-more directories. Pre-existing from round 2, under-reports in the direction of silence (missed drift, not false drift). Fix hypothesis: in the `**` branch, when followed by a literal starting with `/`, also try continuing the match with that leading `/` consumed — or collapse each `**/` to `''` and retry, mirroring the existing `**/${glob}` prefix retry. Add one gate assertion: `src/**/x.ts` matches `src/x.ts`.
- [minor] `workflows/ldo.js` `phaseRecord` forbiddenDocs check (lines 3263-3268): compares Recorder `files_written` raw after only `.trim()` (`f.startsWith('docs/contracts/') || forbiddenDocs.has(f)`), unlike `detectDesignDrift` which normalizes both sides. A Recorder reporting `./docs/design/auth.md`, `docs//design/auth.md`, or an absolute path is not warned about. Advisory-only (non-blocking) and schema says paths are worktree-relative, so this is a hole in an advisory check, not a data-loss path. Fix hypothesis: normalize each `files_written` entry with `normalizeIssuePath` before the `startsWith` test, and use `DESIGN_MAP.some(m => sameFilePath(f, m.doc))` instead of `forbiddenDocs.has(f)`.
**Status 2026-09-19 (2.52.3): FIXED** — `files_written` entries are normalized with `normalizeIssuePath` and matched with `sameFilePath` against the design map, with a comment naming the direction difference from `detectDesignDrift` (workflows/ldo.js:3938-3949).

- [minor] `workflows/ldo.js` `detectDesignDrift` line 2323: `sameFilePath`'s suffix match in both directions lets a changed file whose normalized path is a bare `auth.md` (repo root) suppress drift for a doc named `docs/design/auth.md`. Fails toward silence, though the trigger (a root-level file sharing the doc's basename) is uncommon. Fix hypothesis: for the doc-moved test only, `entry.doc` is already safe/repo-relative, so only one direction is needed — `f.path === entry.doc || f.path.endsWith('/' + entry.doc)` — keeping `sameFilePath` for issue attribution where both sides can be the longer form.
**Status 2026-09-19 (2.52.3): FIXED** — the doc-moved test now matches one direction only — `f.path === entry.doc || f.path.endsWith("/" + entry.doc)` — with a comment explaining that the loose direction would suppress drift (workflows/ldo.js:2882-2888).

- [medium/security] `workflows/ldo.js` design-drift: unbounded/adjacent-wildcard globs (`**/**/**/**/**/**/x.md`) compile to catastrophic-backtracking regex, run up to 40× against a model-authored, length-unbounded `files_changed` array; an unanchored pattern also lets a bare `docs` glob match every path containing that substring. Fix hypothesis: cap glob length/wildcard-token count in `resolveDesignMap`, collapse consecutive `**`/`*` runs before substitution, anchor `^...$`, drop the `g` flag, cap files iterated in `detectDesignDrift`.
**Status 2026-09-19 (2.52.3): FIXED** — no RegExp is compiled from a glob at all — matching is a bounded table walk, anchored, with no `lastIndex` (workflows/ldo.js:2748-2760); `resolveDesignMap` caps glob length and wildcard count, and `detectDesignDrift` iterates at most `DRIFT_FILES_MAX` files (workflows/ldo.js:2871).

- [medium/security] `workflows/ldo.js` design-drift: `doc` validation only rejects absolute paths and `..` segments, admitting a newline/`\r`/leading `#` that could forge a section header in the Record prompt (the class `collapseLines` already guards for `plan.risks`); matched files from model-authored `files_changed` aren't guaranteed collapsed either. Fix hypothesis: validate `doc` with the existing `safeMigrationsDir`/`SAFE_REL_PATH` discipline instead of a second, weaker validator; run `collapseLines()`/`capList()` over every doc/glob/matched-file string before `renderDesignDrift`.
**Status 2026-09-19 (2.52.3): FIXED** — `doc` is validated with `safeMigrationsDir` (workflows/ldo.js:2737-2741), and `renderDesignDrift` collapses `doc`, `glob` and every matched path with `capList` before rendering (workflows/ldo.js:2896-2908).

- [medium/security] `workflows/ldo.js`: `verifyRecordLocation` returns `false` immediately on a single-feature run (the documented default), so there is no orchestrator-side detection of a Recorder writing under `docs/contracts/` or a mapped design doc in the common case — the prohibition lives only in agent prompt text, and `agents/recorder.md`'s Rules section doesn't state it (only section-3 prose does). Fix hypothesis: add detection independent of `ctx.isMulti`, and add the prohibition to the Rules list.
**Status 2026-09-19 (2.52.3): OPEN** — still `if (!ctx?.isMulti) return false` (workflows/ldo.js:3763): the single-feature case has no orchestrator-side detection of a Recorder writing under `docs/contracts/` or a mapped design doc; the prohibition lives only in prompt text.

- [medium/security] The vendored-install fallback path (`.claude/LDO_VENDORED.md`) that the new `/ldo-contract` step points an agent at does not exist — `scripts/vendor.sh` never writes a script path into it. An agent told to run a check it can't locate is exactly the agent that falls back to a cwd-relative `scripts/check-contracts.sh` (the `/ldo-feedback`/redact.sh defect class). Fix hypothesis: drop the LDO_VENDORED.md branch; resolve only `"${CLAUDE_PLUGIN_ROOT:?}/scripts/check-contracts.sh"`, falling back to reading+counting with an explicit "not reachable" message on a vendored install.
**Status 2026-09-19 (2.52.3): FIXED** — skills/ldo-contract/SKILL.md resolves `check-contracts.sh` only through `CLAUDE_PLUGIN_ROOT` and, on a vendored install, reads+counts the contracts itself with an explicit "script was not reachable" message instead of falling back to a cwd-relative script (skills/ldo-contract/SKILL.md:107-110).

- [low] `workflows/ldo.js`: `plan.risks`, `coderResult.deviations` and lines of `verdict.summary` feeding the new contract-signal functions are unbounded in practice; only `collectContractCandidates` is specified with `capList`. Fix hypothesis: run every returned list through `capList()` before logging/rendering; keep `recommendPlanReview`'s string composed from fixed reason text only, never quoted risks/conflicts content.
- [low] Backlog items from the new contract-candidate and design-drift signals reach GitHub under `config.backlog.destination: "github"` with no redaction step, unlike `/ldo-feedback`'s `scripts/redact.sh` path. Structural fix (redact the backlog path the way `/ldo-feedback` redacts its body) is larger work; interim prompt-level mitigation (state rules abstractly, never quote credentials/tokens/endpoints/identifiers) has been added to the CONTRACT CANDIDATES block and `agents/coder.md`/`agents/reviewer.md`.

## 2026-09-07

- [minor] `workflows/ldo.js` `readSpent` (~line 2054, per-phase cost accounting): the shape check `typeof budget.spent !== 'function'` runs outside the try/catch, so a `budget` whose `spent` is a throwing getter escapes `readSpent`/`createCostLedger` entirely and fails the whole run over an accounting figure. Reproduced with a throwing-getter `spent` property. Contrived (a real harness object has no getter) but breaks the "open/close/finish must never throw" guarantee. Fix hypothesis: move the `!budget || typeof budget.spent !== 'function'` test inside the try block (keep `typeof budget === 'undefined'` first, since it can't throw); add a throwing-getter case to `scripts/check-cost-accounting.sh`'s NEVER-BREAKS-A-RUN assertion.
- [minor] `workflows/ldo.js` `renderCost`: unconditionally appends "the run log and the returned result carry a later, larger total that does" even when the cost block is unavailable/partial and nothing was measured, producing a contradictory pair of sentences in the same Record-prompt block. Fix hypothesis: condition that sentence on `Number.isFinite(cost.total_output_tokens_delta)`, and drop "larger" since a real delta could legitimately be 0.
- [minor] `workflows/ldo.js` (pre-existing, not introduced by cost accounting): `runOneFeature`'s first log line and the two summary lines (~3734, 3901, 3938) reference the `budget` global bare rather than through the new `typeof budget === 'undefined'` guard. Line 3734 runs before `ctx.ledger` is created, so a future harness that stops injecting `budget` would die with a `ReferenceError` there before `readSpent`'s guard is ever reached — the "budget absent" path the new gate drives isn't reachable end-to-end in a real run today. Fix hypothesis: route those three sites through the same guard or a small `budgetTotal()` helper.
- [nit] `workflows/ldo.js` `costFigure`: `String(Math.round(n))` renders exponent notation (`1e+21`) for deltas at or above 1e21. Unreachable for real output-token counts (`Number.MAX_SAFE_INTEGER` ≈ 9e15); worth a `toLocaleString`/`BigInt` fix only if ever relied on for very large synthetic figures.
